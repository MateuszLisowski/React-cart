import React from 'react';
import CartItems from './CartItems';
import { shallow } from 'enzyme';

describe('<CartItems />', () => {
    //const wrapper = shallow(<CartItems cartItems={} buyItems={() => { }} removeItem={() => { }} />);
    it('work', () => {
        expect(1).toBe(1);
    })
})